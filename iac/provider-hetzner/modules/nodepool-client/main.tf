# Client (orchestrator/build) node pool will be implemented in a later step.
# Will create hcloud_server instances for Firecracker orchestrator workloads.
# Requires dedicated vCPU instances (ccx* or bare metal) for nested virtualization.
